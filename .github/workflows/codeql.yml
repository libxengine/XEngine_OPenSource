name: CodeQL Advanced

on:
  push:
    branches: 
      - 'master'

jobs:
  analyze:
    runs-on: ubuntu-24.04
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: c-cpp
          build-mode: manual
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7
          
    - name: Set TERM variable
      run: |
          echo "TERM=xterm" >> $GITHUB_ENV

    - name: install library package
      run: |
          sudo apt install libluajit-5.1-dev -y
            
    - name: Set up Dependency Environment
      run: |
          latest_tag=$(curl -s -H "Authorization: Bearer ${{ github.token }}" https://api.github.com/repos/libxengine/libxengine/releases/latest | jq -r .tag_name)
          wget https://github.com/libxengine/libxengine/releases/download/$latest_tag/XEngine_UBuntu_24.04_x86-64.zip
          unzip ./XEngine_UBuntu_24.04_x86-64.zip -d ./XEngine_UBuntu_24.04_x86-64
          cd XEngine_UBuntu_24.04_x86-64

          chmod +x ./XEngine_LINEnv.sh
          sudo ./XEngine_LINEnv.sh -i 3

    - name: make pre
      run: | 
            cd XEngine_Module
            cd jsoncpp
            make
            cd ..
            cd tinyxml2
            make
            cd ..

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: manual 
        queries: security-and-quality
        
    - name: make build
      run: | 
            cd XEngine_Module
            cd XEngine_InfoReport
            make
            cd ..
            cd XEngine_Token
            make
            cd ..
            cd XEngine_MSGNotify
            make
            cd ..
            cd XEngine_AIApi
            make
            cd ..
            cd XEngine_Verification
            make
            cd ..
            cd XEngine_PluginExtension
            make

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{ matrix.language }}"
